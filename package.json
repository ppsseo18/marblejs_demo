{
  "name": "marblejs_demo",
  "version": "0.0.1",
  "description": "Demo application of Marble.js",
  "main": "dist",
  "scripts": {
    "start": "node dist",
    "dev": "NODE_ENV=development nodemon --watch 'src/**/*' -e ts,tsx --exec 'ts-node' ./src",
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist"
  },
  "author": "kyongmin.noh",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.12.2",
    "@types/ramda": "github:types/npm-ramda#dist",
    "nodemon": "^1.18.6",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "@marblejs/core": "^1.2.0",
    "@marblejs/middleware-body": "^1.2.1",
    "@marblejs/middleware-logger": "^1.2.1",
    "chalk": "^2.4.1",
    "ramda": "^0.25.0",
    "rxjs": "^6.3.3"
  }
}
